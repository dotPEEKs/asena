# This file created for all window frame constants

from enum import IntEnum

class MainWindowFrameConstants(IntEnum):
    LEFT_FRAME_CONTAINER_MINIMUM_WIDTH_SIZE = 60
    LEFT_FRAME_CONTAINER_MAXIMUM_WIDTH_SIZE = 300
